/**
 * Horizontal alignment with `Nav`: same outer inset + max width as the header shell.
 */
export const PAGE_EDGE = "px-3 sm:px-5 lg:px-10";

export const PAGE_MAX =
  "mx-auto w-full max-w-[min(1240px,calc(100%-1.5rem))] sm:max-w-[min(1320px,calc(100%-2.5rem))]";
